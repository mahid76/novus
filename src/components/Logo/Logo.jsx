import React from 'react'

const Logo = ({className}) => {
  return (
   

    <div>
      <p className={`font-secondary font-bold text-[24px] tracking-[3%]  ${className}` }>
        Novous
      </p>
    </div>
  )
}

export default Logo
